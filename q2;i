a) Java allows classes to inherit attributes and methods from other classes using the extends keyword.

Required:

i) Create a base class named Vehicle with the following specifications: (8 Marks)

The clas